version: 2,
jobs:
  build:
    - image: circleci/node:8.1

  steps:
    - checkout
    - run:
        name: Install node modules
        command: npm install
    - run:
        name: Lint
        command: npm run lint
    - run:
        name: Test
        command: npm run test
    
