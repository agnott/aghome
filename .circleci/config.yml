version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.8.1

  steps:
    - checkout
    - run:
        name: Install Node Modules
        command: npm install
    - run:
        name: Lint
        command: npm run lint
    - run:
        name: Test
        command: npm run test
